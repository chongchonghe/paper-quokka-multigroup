#!/bin/bash
# Environment setup script

# Set the PYTHONPATH to include the shared_code folder
export PYTHONPATH=$(pwd)/shared_code:$PYTHONPATH

# Activate Conda environment
#conda activate project_env
